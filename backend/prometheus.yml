global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "fastapi-metrics"
    scrape_interval: 5s
    metrics_path: "/metrics"

    authorization:
      type: "Bearer"
      credentials_file: "/run/secrets/metrics_token"

    static_configs:
      - targets: ["api:8000"]