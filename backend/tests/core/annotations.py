from typing import TypeVar

PathStr = TypeVar("PathStr", bound=str)
MethodStr = TypeVar("MethodStr", bound=str)
UserRoleStr = TypeVar("UserRoleStr", bound=str)
InvalidField = TypeVar("InvalidField", bound=str)
